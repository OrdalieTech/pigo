<b>Release check</b>

The build is <b>green</b> &amp; ready — see <i>notes</i> with <code>go test ./...</code> for 1 &lt; 2 and A &gt; B.

• first step
• second step with <a href="https://example.com/x?a=1&amp;b=2">a link</a>
  • nested detail
• <s>dropped</s> kept

1. compile
2. ship

<blockquote>quoted wisdom
on two lines</blockquote>

<pre><code class="language-go">if a &lt; b &amp;&amp; c &gt; d {
	fmt.Println(&#34;&lt;ok&gt;&#34;)
}</code></pre>

Visit https://example.com for more.

———

Done.