# Release check

The build is **green** & ready — see *notes* with `go test ./...` for 1 < 2 and A > B.

- first step
- second step with [a link](https://example.com/x?a=1&b=2)
  - nested detail
- ~~dropped~~ kept

1. compile
2. ship

> quoted wisdom
> on two lines

```go
if a < b && c > d {
	fmt.Println("<ok>")
}
```

Visit https://example.com for more.

---

Done.
