# Deploy report

The build **succeeded** with __zero__ warnings & 3 skips.

*Latency* dropped ~~20%~~ 25% — see [the dashboard](https://grafana.example.com/d/1?from=now-7d&to=now).

- first item
- second item with `inline code`
  - nested item
* starred item

1. ordered one
2. ordered two

> quoted wisdom
> about a < b

Compare a < b and b > c, then run ```ls -la``` to check.

```go
if a < b && c > d {
	fmt.Println("x & y")
}
```
