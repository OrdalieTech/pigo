# Deploy report

This is **bold**, this is *italic*, this is __also bold__, and ~~removed~~.

See the [runbook](https://example.com/runbook?step=2) and `kubectl get pods`.

- first item with **bold**
- second item
  - nested item

> a quoted **warning** line

```go
if **not converted** && [not](a-link) {
    fmt.Println("verbatim")
}
```

## Follow-up

Inline ```ls -la``` span stays usable.
