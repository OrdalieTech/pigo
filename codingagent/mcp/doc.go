// Package mcp implements pigo's bundled, settings-driven MCP extension.
// It is dormant when no enabled servers are configured and exposes remote
// tools only through the native extension API.
package mcp
