# M5 trim and verification report — release candidate

Status: **all locally executable verification is complete, including the fresher upstream lock
bump, but M5 cannot close while the D17-complete binary is 51,703,970 B against a 35 MB cap.** The
other open boxes are owner-gated: subscribed-account OAuth, CI secrets and the 72-hour window,
clean-VM install verification, and tag publication. Nothing was weakened; PROGRESS.md records the
exact unblocking step for each open item.

## Revisions and tools

- Candidate: the Sprint 4 closure containing this report (descends from `529035c`).
- upstream: pi `0.80.10`, `3a40794ea14c6202586cc203d5b928eca9f6b673`, promoted 2026-07-20 after
  classifying all 116 changed paths, regenerating fixtures, and passing the full race suite.
- Go `1.26.2`; Node `24.15.0`; golangci-lint `2.7.2`; dupl `1.1.0`; hyperfine `1.18.0`;
  goreleaser `2.6.1`.

## Final measurements

| Metric | Value | Budget | Verdict |
|---|---:|---:|---|
| Mirror LOC combined | 107,592 / 99,172 = **1.085x** | ≤ 1.3x | Green (AI 1.060x, agent 0.952x, coding-agent 1.157x, TUI 0.900x) |
| `dupl -t100` production groups | 20 | judged, not counted | Protocol/rendering branches; the new report is the existing agent/faux deep-copy shape crossing the threshold, not an earned shared API |
| Stripped binary (bridged) | **51,703,970 B** | ≤ 35 MB | **Red.** Embedded sobek + esbuild are required by D17; closure needs an owner decision rather than dropping parity |
| Cold start, quiet machine, 100 runs | **45.0 ± 5.2 ms** (min 39.3) | < 50 ms | **Green with thin margin.** Corrects M4: the 50.4–50.9 ms means were measured under concurrent tooling load; controlled re-runs sit at 44–45 ms. Load-sensitive — recorded, not hidden. |
| Bridge call latency | p90 137 µs | < 8 ms | Green (test-guarded) |
| Frame replay | median 302 µs, p90 513 µs | < 16 ms | Green |
| `go mod verify` / `tidy -diff` | clean | — | Green; deps within ARCHITECTURE §8 |

## Release machinery evidence

- goreleaser snapshot built all four targets with checksums; the linux/amd64 archive was
  checksum-verified, extracted, and the binary runs and reports the ldflags-injected version.
- The install script's mechanics (platform resolve, checksum verify, extract, install) were
  exercised against the snapshot artifacts; clean-VM end-to-end runs need owner hosts.
- The release workflow re-runs `make check` at the tag and extracts notes from CHANGELOG.md.
- Sync machinery and the literal lock bump ran GREEN end-to-end against fresher commit `3a40794e`
  (classify, regenerate, full race suite, promote); the generated report records zero unmapped
  paths and the promoted fixture manifests.

## Alignment audit at the release candidate

Re-run verdict in `docs/compare/upstream-alignment.md`: **all 22 confirmed should-fix findings are
closed; D29 records the AgentHarness facade as dissolved into the existing runtime.** Closing the
tails surfaced and fixed real defects beyond bookkeeping: the CLI never passed the stream SessionID (prompt-cache
keys and affinity headers missing from every real run), live extension custom messages were
dropped, the select-list theme crashed under any active theme, and 28 of upstream's `app.*` legacy
keybinding migrations were missing.

## M1–M4 re-verification at the release candidate

`make fixtures-check` regenerates the full fixture tree byte-clean at the pinned lock; the
upstream RPC suite passes 28/28; `make check` (build, vet, lint, full race suite) is green; the
four cross-builds compile. M1 dogfood, M2 compare/trim, M3 compare/trim, M4 compare/trim reports
stand unchanged with their evidence. The two owner-blocked M2 boxes (subscribed Anthropic login,
first hosted nightly run) and the owner-blocked M3 box (real-terminal image/clipboard smoke)
remain accurately unchecked.

## Release remainder (exact unblocking steps in PROGRESS.md)

1. Binary-size cap decision — amend D17 or the 35 MB criterion; the measured bridged artifact is
   51,703,970 B and remains red.
2. Subscribed-account OAuth end-to-end runs (Anthropic Pro/Max, ChatGPT/Codex, Copilot, xAI).
3. CI secrets and publication access → first hosted nightly, then the trailing-72-hour ≥90% window.
4. Clean linux/macOS hosts → install-script and docs-path end-to-end verification.

The annotated `v0.1.0` tag exists locally; publishing it remains gated by items 1–4.
