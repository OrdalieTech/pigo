# M5 trim and verification report — release candidate

Status: **every locally provable criterion across M1–M5 is green at the release candidate; the
remaining boxes are owner-gated** (subscribed-account OAuth runs, CI secrets and the 72-hour
window, clean-VM install verification, the binary-size cap decision, an upstream commit newer than
the pin to bump to, and the tag itself). Nothing was weakened: every open item carries its exact
unblocking step in PROGRESS.md.

## Revisions and tools

- Candidate: the Sprint 4 closure containing this report (descends from `529035c`).
- upstream: pi `0.80.10`, `3da591ab74ab9ab407e72ed882600b2c851fae21` — still `origin/main` HEAD at
  audit time, so the pin IS upstream's newest commit.
- Go `1.26.2`; Node `24.15.0`; golangci-lint `2.7.2`; dupl `1.1.0`; hyperfine `1.18.0`;
  goreleaser `2.6.1`.

## Final measurements

| Metric | Value | Budget | Verdict |
|---|---:|---:|---|
| Mirror LOC combined | 107,454 / 98,662 = **1.089x** | ≤ 1.3x | Green (AI 1.152x, agent 0.940x, coding-agent 1.113x, TUI 0.896x) |
| `dupl -t100` groups | 19 | judged, not counted | All protocol/rendering branches; no actionable clone |
| Stripped binary (bridged) | **51,478,690 B** | ≤ 35 MB | **Owner decision pending** (expansion-study decision 3; bridge-less variant measured 35.1 MB) |
| Cold start, quiet machine, 100 runs | **45.0 ± 5.2 ms** (min 39.3) | < 50 ms | **Green with thin margin.** Corrects M4: the 50.4–50.9 ms means were measured under concurrent tooling load; controlled re-runs sit at 44–45 ms. Load-sensitive — recorded, not hidden. |
| Bridge call latency | p90 137 µs | < 8 ms | Green (test-guarded) |
| Frame replay | median 302 µs, p90 513 µs | < 16 ms | Green |
| `go mod verify` / `tidy -diff` | clean | — | Green; deps within ARCHITECTURE §8 |

## Release machinery evidence

- goreleaser snapshot built all four targets with checksums; the linux/amd64 archive was
  checksum-verified, extracted, and the binary runs and reports the ldflags-injected version.
- The install script's mechanics (platform resolve, checksum verify, extract, install) were
  exercised against the snapshot artifacts; clean-VM end-to-end runs need owner hosts.
- The release workflow re-runs `make check` at the tag and extracts notes from CHANGELOG.md.
- Sync machinery: one full cycle ran GREEN end-to-end (fetch, classify, regenerate, full race
  suite on the regenerated tree) after fixing the tool's cgo-disabled race invocation; the literal
  lock bump is impossible until upstream publishes a commit newer than the pin.

## Alignment audit at the release candidate

Re-run verdict in `docs/compare/upstream-alignment.md`: **21 of 22 confirmed should-fix findings
closed; the one open item is the AgentHarness facade owner decision.** Closing the tails surfaced
and fixed real defects beyond bookkeeping: the CLI never passed the stream SessionID (prompt-cache
keys and affinity headers missing from every real run), live extension custom messages were
dropped, the select-list theme crashed under any active theme, and 28 of upstream's `app.*` legacy
keybinding migrations were missing.

## M1–M4 re-verification at the release candidate

`make fixtures-check` regenerates the full fixture tree byte-clean at the pinned lock; the
upstream RPC suite passes 27/27; `make check` (build, vet, lint, full race suite) is green; the
four cross-builds compile. M1 dogfood, M2 compare/trim, M3 compare/trim, M4 compare/trim reports
stand unchanged with their evidence. The two owner-blocked M2 boxes (subscribed Anthropic login,
first hosted nightly run) and the owner-blocked M3 box (real-terminal image/clipboard smoke)
remain accurately unchecked.

## Owner-gated remainder (exact unblocking steps in PROGRESS.md)

1. Binary-size cap decision (and the thin cold-start margin it buys back) — expansion-study
   decision 3.
2. Subscribed-account OAuth end-to-end runs (Anthropic Pro/Max, ChatGPT/Codex, Copilot, xAI).
3. Repository remote + CI secrets → first hosted nightly, then the trailing-72-hour ≥90% window.
4. Clean linux/macOS hosts → install-script and docs-path end-to-end verification.
5. An upstream commit newer than the pin → the green lock bump.
6. AgentHarness facade decision.
7. `git tag v0.1.0` once 1–5 close — the tag command is the only remaining mechanical act.
