# Sprint 5 trim and verification report — chat gateway

Status: **the deterministic D27 surface is green**. Chat is an SDK addition with no TS-pi mirror,
so the report records its absolute footprint and reference comparison instead of inventing an
upstream LOC ratio.

## Scope and measurements

- Sprint closure: `43e5863`; current trim refresh: release closure descending from `e46f671`.
- Scope: `chat/` core, Telegram, WhatsApp, `chat/examples/localbot`, and the small
  `AgentSessionOptions.ToolOptions` SDK seam.
- Current D27 chat footprint: **4,291 product lines / 3,967 test lines**. The SDK seam is excluded
  because it shares files with the upstream-mirrored runtime.
- Closing readability pass against `e46f671`: **75 additions / 88 deletions, net 13 lines removed**.
- `staticcheck` reports zero findings and `dupl -t100` reports zero clone groups in the D27 paths.
- Direct dependency growth: **zero**. No package outside `chat/` imports chat, so the CLI links
  **zero chat bytes** and inherits no chat startup cost.

## Trim checklist

1. **Dead code:** staticcheck is clean; no suppression or compatibility scaffold was added.
2. **Dependencies:** the gateway uses the standard library plus existing pi-go packages; `go.mod`
   and `go.sum` did not change for D27.
3. **Duplication:** the final D27 scan reports zero clone groups; shared behavior remains local
   until a second real consumer earns extraction.
4. **Abstractions:** only the frozen `Adapter`, `Delivery`, provider, and ledger seams remain; each
   has multiple platform or persistence consumers.
5. **LOC:** absolute addition size is recorded above; the ≤1.3× mirror budget does not apply to a
   documented addition with no upstream TypeScript package.
6. **Size and speed:** chat is outside the `cmd/pi` import graph. The race-clean 1,000-turn test over
   100 conversation keys covers serialization throughput and verifies zero idle lock residue.
7. **Milestone:** every deterministic Sprint 5 definition-of-done item is checked in
   `docs/plan/PROGRESS.md`; no credential-dependent test is part of D27 closure.
8. **Alignment:** `docs/compare/sprint-5.md` records the pi-chat/Hermes cross-check and every
   deliberate difference; `conformance/` remains untouched.

## Verification

```text
XDG_CACHE_HOME=/tmp/pi-go-xdg GOCACHE=/tmp/pi-go-gocache \
  .tools/bin/staticcheck ./chat ./chat/telegram ./chat/whatsapp ./chat/examples/localbot
dupl -t 100 chat/*.go chat/telegram/*.go chat/whatsapp/*.go chat/examples/localbot/*.go
go test -race ./chat/...
CGO_ENABLED=0 go build ./...
```

The sprint closure recorded the full race/build gate green; the final combined release gate
re-runs it after the current behavior-neutral trim.
