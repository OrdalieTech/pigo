# Upstream sync report — 2026-07-21

## Revisions

- Previous lock: pi v0.80.10 at `3a40794ea14c6202586cc203d5b928eca9f6b673`.
- Target lock: pi v0.81.0 at `9c480b6ad2c7419875a7a850fb4ad5f9232313b8`.
- Target identity was verified with GitHub CLI against the annotated release tag and an exact
  detached checkout in `/tmp`; the pinned `.upstream` v0.80.10 specification checkout was not
  rewritten during comparison.
- Upstream changed 138 paths: 27 additions, one deletion, 94 ordinary modifications, and 16
  orchestrator-to-server renames.

GitHub CLI also showed that v0.81.1 was published while this target-bound sync was in progress. The
requested Kimi K3 compatibility fix (`959cc1897e`) is retained as an explicit ahead-of-pin SYNC-1
backport. The remaining v0.81.1 retry, model, and packaging changes are not attributed to v0.81.0;
they belong to the next sync cycle.

## In-scope delta

- `packages/agent`: required stream-function injection; public versus coding compaction result
  contracts; exact retained-tail propagation; session checkpoint-path API and storage adaptations.
- `packages/ai`: strict model/image data validation, live NVIDIA/OpenRouter/Vercel catalog sources,
  full 39-model image snapshot, model-store freshness metadata, and all provider parity corrections
  enumerated in the comparison report.
- `packages/coding-agent`: v0.81 product changelog and version surfaces, searchable login selector,
  browser launch, completion/auth guidance, runtime stream injection, and deferred catalog behavior.
- Development/release surface: immutable action updates, extractor API renames, deterministic
  same-directory generated-file replacement, exact-tag read-only fixture generation, and lock/docs.

The exhaustive behavior evidence is in
[`docs/compare/provider-login-parity-2026-07-21.md`](../../compare/provider-login-parity-2026-07-21.md).
All 52 requested IDs finish CONFIRMED; no INSUFFICIENT or REGRESSION verdict remains.

## Catalog evidence

- NVIDIA NIM is the exact ordered 19-ID `nvidia.models.ts` manifest, intersected with the captured
  live listing. The Go-only `qwen/qwen3.5-122b-a10b` denylist entry is ledgered.
- OpenRouter has 271 IDs: the v0.81.0 269-ID manifest plus exactly two tool-capable live additions,
  `google/gemini-3.5-flash-lite` and `google/gemini-3.6-flash`. Sorted-ID digest:
  `76a3833444dbe6dab7a6edd4fd29915efe3047634b0b8909eb73111086ca0cb4`.
- Vercel AI Gateway has 190 IDs: the v0.81.0 188-ID manifest plus the same two tool-use-tagged live
  additions. Sorted-ID digest: `312246de110840fbe5eea7ed506dd387aaebd77f26e3c7e01e868220808658a2`.
- OpenCode has 53 IDs and no `hy3-free`; both Qwen Token Plan catalogs have
  `qwen3.8-max-preview`; the complete image catalog has 39 models and digest
  `40c176ec0df9d315d0f5920a3ed4550e084daa9e07533a838f5464e1135a3106`.

## Fixture regeneration

All 30 fixture manifests were generated from the exact v0.81.0 checkout. Payload changes outside
the provider F2 parity additions are limited to the upstream `/changelog` digest/tail and the
v0.80.10-to-v0.81.0 banner in WP450 replay/UI demos. F10 compaction payloads are byte-identical;
only their generated manifest provenance changed.

`make fixtures-check UPSTREAM_READONLY=1 UPSTREAM_DIR=/tmp/pi-upstream-v0.81.0` is GREEN, so the
committed fixture tree is byte-clean without mutating `.upstream`.

## Explicit exclusions and adaptations

- `packages/server` remains the experimental side-product exclusion formerly named
  `packages/orchestrator`.
- `packages/storage/sqlite-node` and the optional llama extension require native Node runtimes and
  remain excluded by D7; JSONL/memory session contracts and public repository interfaces are ported.
- The runtime catalog endpoint remains direct models.dev under D12, with upstream
  `checkedAt`/`lastModified` semantics and the D30 `pigo` User-Agent substitution.
- `pigo login/logout`, bare-logout behavior, the NVIDIA denylist, the Kimi backport, and the direct
  `github.com/klauspost/compress` zstd dependency are recorded in `docs/DECISIONS.md`.

## Verification gates

The final tree passes:

```text
go mod tidy -diff
make check
make fixtures-check UPSTREAM_READONLY=1 UPSTREAM_DIR=/tmp/pi-upstream-v0.81.0
git diff --check
```

`make check` includes the static `CGO_ENABLED=0` product build, `go vet`, golangci-lint with zero
findings, and `go test -race ./...`. The gate itself found and forced correction of an obsolete
Codex helper plus two stale v0.80.10 runtime version constants before this report was marked green.
