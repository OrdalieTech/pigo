# Sprint 1 comparison — core headless

Status: **GREEN for deterministic Sprint 1 parity**. The same pinned TypeScript scenarios now
produce byte-identical print, JSON, and RPC output in Go, and every locally runnable M2 criterion
is green. M2 itself remains open only for the subscribed Anthropic Pro/Max login and the first
hosted nightly run, which require owner credentials and infrastructure.

This report supersedes the integration-time gap lists in `wp-340-report.md`, `wp-350-report.md`,
`wp-351-report.md`, and `wp-370-report.md`.

## Revisions and method

- pi-go base: `1958a78f34aeaf07a2fc60925e566bc3a3818e5f` (Sprint 0 closure); candidate: the
  direct-main Sprint 1 closure containing this report.
- upstream: pi `0.80.10`, `3da591ab74ab9ab407e72ed882600b2c851fae21`, matching both
  `UPSTREAM.lock` and the clean detached `.upstream/` checkout.
- Every golden was regenerated by its TypeScript extractor. The only F7-cli canonicalizations are
  the temporary project path, generated JSON header timestamp, and package installation path,
  all declared in its manifest.
- No live provider or OAuth request is claimed. Provider protocol evidence is pinned fixture data;
  the two owner-run checks are listed below.

## Identical scripted modes

| Mode | TypeScript and Go route | Result |
|---|---|---|
| Print text | `F7-cli` runs the real pinned TS CLI and the real Go binary with the same piped input, CLI prompt, temporary `models.json`, and local OpenAI-compatible SSE stream. It compares exit status, stdout, stderr, request count, and the exact combined prompt. The missing-model case compares the complete diagnostic bytes. | **GREEN.** Success output, Unicode, LF framing, stdin concatenation, and missing-model exit/error bytes match. |
| JSON | `F3-session` drives pinned `runPrintMode` and the Go JSON route through six faux-provider scenarios comprising 121 JSONL records. `F7-cli` separately compares the real CLI session header and missing-model diagnostic. | **GREEN.** Multiple prompts, queueing, retry, compaction failure, assistant error, abort, header emission, and failure exit behavior match. |
| RPC transcript | `F7` replays 40 commands and 54 raw JSONL records in-process and through the Go binary with no payload canonicalization. `F7-cli` adds a real missing-model `get_state` plus `prompt` sequence and compares the exact unknown-model state and rejected-prompt frame. | **GREEN.** Commands, events, response ordering, strict LF/CRLF behavior, EOF dispatch, and missing-model bytes match. |
| Upstream RPC suite | The adapter runs six pinned upstream test files unchanged against `pi-go --mode rpc`, substituting only the executable and the suite's local Anthropic SSE endpoint. | **GREEN: 6 files, 27/27 tests, zero exclusions.** |

## Core surface comparison

| Surface | Result and evidence |
|---|---|
| Landed provider shapes | **GREEN.** F2 covers OpenAI Responses/Completions, Anthropic, Google, Vertex, Mistral, Azure, Bedrock, and pi-messages. Codex and the compat family remain Sprint 3 under D26. |
| Auth storage and lifecycle | **GREEN locally.** The TypeScript verifier reads Go `auth.json` and exercises cross-runtime locking. Interactive login/logout are tied to mode cancellation, and `RefreshAuth` rebuilds credential-dependent projections without rereading unrelated `models.json`. |
| Harness | **GREEN.** F6Harness covers FileSystem/ExecutionEnv, memory and JSONL repositories, delayed exclusive flush, legacy migration, future-member preservation, and rehydrate-from-bytes through runtime new/switch/fork/import flows. |
| Skills and templates | **GREEN.** Expanded F8 covers precedence, canonical dedupe, source metadata, collision/diagnostic order, command visibility and collisions, harness substitution quirks, input ordering, and immediate `DefaultResourceLoader.ExtendResources`; F9 and F10 remain green. |
| Native extension seams | **GREEN.** Expanded F11-native covers nil-handler diagnostics, panic origin, provider queue/isolation/post-bind behavior, project-trust ordering, and input identity. F11-wire keeps provider, tool, input, command, resource, compaction/tree, and session replacement wiring green. JavaScript execution remains Sprint 3. |
| SDK facade | **GREEN.** Public tests compile the upstream-shaped session controls, service factories, reloadable `ResourceLoader`, provider registration, reload options, and extension flags from an external package. All 13 examples compile and run on faux with isolated configuration; a fresh external module resolves the public package, builds CGO-free, and `go doc -all` enumerates the API. |

The resource loader now applies extension-discovered skills, prompts, and themes immediately. Skill
and prompt diagnostics remain separate at the public seam and retain upstream order when combined
for the legacy resource snapshot. Full theme-object/source-info installation belongs to Sprint 2's
interactive registry and is recorded there rather than hidden behind a headless claim.

## Verification

The exact candidate passes:

```text
make fixtures-check
make upstream-rpc-tests                         # 6 files, 27/27
make build
make test                                       # go test -race ./...
make lint                                       # vet + golangci-lint, 0 issues
go mod verify
go mod tidy -diff
CGO_ENABLED=0 GOOS={linux,darwin} GOARCH={amd64,arm64} go build ./...
```

All 13 examples also run sequentially with isolated `HOME`, `PI_CODING_AGENT_DIR`, `PI_OFFLINE=1`,
and the faux provider. `docs/trim/M2.md` records the shrink, dependency, LOC, binary-size, and
cold-start evidence.

## M2 disposition

Six of eight binding M2 criteria are green:

| Criterion | Status |
|---|---|
| F2 for landed core shapes | **GREEN** |
| Anthropic OAuth plus `auth.json` compatibility | **OWNER-BLOCKED:** `auth.json` is green; a subscribed Pro/Max browser login and streamed request have not been available. |
| Harness SessionRepo/FileSystem parity | **GREEN** |
| Upstream RPC suite and F7 | **GREEN** |
| F8, F9, F10 | **GREEN** |
| SDK examples and external consumer | **GREEN** |
| Nightly live suite wired and running | **OWNER-BLOCKED:** workflow and corpus are committed; no remote/secrets exist for the first hosted run. |
| Trim pass 2 | **GREEN** |

There is no deterministic Sprint 1 divergence to ledger. The two unchecked release boxes are not
waived or weakened; local work advances to Sprint 2 while their owner evidence remains open.
